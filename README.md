# Cadeia de Markov

Siga os passos abaixo para executar o projeto corretamente:

## 1. Compilação

Execute o comando abaixo para compilar o codigo:

```bash
gcc main.c -o run
```


## 2. Executar o projeto

Por fim, rode o programa:

```bash
.\run.exe
```

